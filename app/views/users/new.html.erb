<div class="hero">
  <p class="large brandon">sold is coming</p>
  <p class="small brandon">we're turning shopping into a game<br>...in the palm of your hand</p>
</div>

<div class="form-wrap clearfix <%= "ended" if Rails.application.config.ended %>">
  <% if Rails.application.config.ended %>
    <%= render "users/campaign_ended" %>
  <% else %>
    <%= render "users/campaign_ongoing" %>
  <% end %>
</div>